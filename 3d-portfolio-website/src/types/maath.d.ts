declare module 'maath/random/dist/maath-random.esm' {
  export function inSphere(array: Float32Array, { radius }: { radius: number }): Float32Array;
  export function inCircle(array: Float32Array, { radius }: { radius: number }): Float32Array;
  // Add other functions as needed
}
